version: '3'

services:
  traefik:
    image: traefik
    restart: always
    networks:
      - gateway
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./cert:/cert
      - ./traefik.toml:/traefik.toml

networks:
  gateway:

